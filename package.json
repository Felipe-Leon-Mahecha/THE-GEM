{
  "name": "the-gem",
  "version": "1.0.0",
  "description": "Esfera Inversa",
  "main": "main.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/copy-runtime-assets.mjs",
    "check": "node --check powerups.js && node --check src/main.js && node --check shop.js && node --check trails.js && node --check levelselect.js && node scripts/check-runtime-assets.mjs",
    "asset-report": "node scripts/asset-report.mjs",
    "deploy": "sh deploy.sh"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor-firebase/authentication": "^8.3.0",
    "@capacitor/android": "^8.4.0",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "firebase": "^12.15.0",
    "phaser": "^3.90.0",
    "vite": "^8.0.16"
  }
}
